<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Erro - Furina do Discord</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Raleway:wght@400;600&display=swap" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Raleway', sans-serif;
      background: radial-gradient(ellipse at top, #1c2331 0%, #0a0f1c 70%);
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      height: 100vh;
      margin: 0;
      padding: 2rem;
    }

    .error-box {
      background-color: rgba(255, 255, 255, 0.04);
      border: 2px solid #3DD1D9;
      border-radius: 1rem;
      padding: 2rem;
      max-width: 600px;
      animation: pulse 2s ease infinite;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      color: #3DD1D9;
      margin-bottom: 1rem;
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 1rem;
    }

    a {
      display: inline-block;
      margin-top: 1rem;
      padding: 0.6rem 1.4rem;
      background-color: #3DD1D9;
      color: #081d35;
      font-weight: bold;
      border-radius: 1rem;
      text-decoration: none;
      transition: background-color 0.3s;
    }

    a:hover {
      background-color: #29b8c3;
    }

    @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(61, 209, 217, 0.4); }
      70% { box-shadow: 0 0 15px 10px rgba(61, 209, 217, 0); }
      100% { box-shadow: 0 0 0 0 rgba(61, 209, 217, 0); }
    }
  </style>
</head>
<body>
  <div class="error-box">
    <h1>Oh non non non...</h1>
    <p><%= message %></p>
    <a href="/dashboard/<%= guild.id %>/embed">Voltar para a página de Embed</a>
  </div>
</body>
</html>
