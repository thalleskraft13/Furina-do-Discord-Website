<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Furina do Discord - Servidores</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Raleway:wght@400;600&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Raleway', sans-serif;
      background: radial-gradient(ellipse at top, #0b1c3a 0%, #08172e 70%, #06152a 100%);
      color: white;
      overflow-x: hidden;
      padding-bottom: 5rem;
    }

    header {
      background-color: rgba(6, 21, 42, 0.9);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .logo {
      display: flex;
      align-items: center;
      font-size: 1.5rem;
      color: #3DD1D9;
      font-weight: bold;
      font-family: 'Playfair Display', serif;
    }

    .logo img {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 10px;
      border: 2px solid #3DD1D9;
    }

    .content {
      padding: 3rem 2rem;
      text-align: center;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: #3DD1D9;
    }

    .update-btn {
      padding: 0.6rem 1.5rem;
      background-color: #3DD1D9;
      color: #081d35;
      border: none;
      border-radius: 1.5rem;
      font-weight: 600;
      cursor: pointer;
      margin-bottom: 2rem;
      transition: 0.3s ease;
    }

    .update-btn:hover {
      background-color: #29b8c3;
    }

    .guilds {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
    }

    .guild {
      background-color: rgba(255, 255, 255, 0.03);
      border: 2px solid #3DD1D9;
      padding: 1rem;
      border-radius: 1rem;
      width: 220px;
      text-align: center;
      transition: transform 0.3s;
    }

    .guild:hover {
      transform: scale(1.05);
    }

    .guild img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      border: 3px solid #3DD1D9;
      margin-bottom: 0.5rem;
      object-fit: cover;
    }

    .guild-name {
      color: #ffffff;
      font-weight: bold;
      margin-bottom: 0.5rem;
    }

    .guild a {
      display: inline-block;
      margin-top: 0.5rem;
      padding: 0.5rem 1rem;
      background-color: #3DD1D9;
      color: #081d35;
      border-radius: 1rem;
      text-decoration: none;
      font-weight: 600;
    }

    .no-guilds {
      font-size: 1.2rem;
      color: #ccc;
      margin-top: 2rem;
    }

    @media (max-width: 600px) {
      .guild {
        width: 100%;
      }

      .update-btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <header>
    <div class="logo">
      <% if (user.avatar) { %>
  <img 
    src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.<%= user.avatar.startsWith('a_') ? 'gif' : 'png' %>" 
    alt="Avatar de <%= user.username %>" 
    width="100" height="100" />
<% } else { %>
  <img 
    src="https://cdn.discordapp.com/embed/avatars/<%= parseInt(user.discriminator) % 5 %>.png" 
    alt="Avatar padrão de <%= user.username %>" 
    width="100" height="100" />
<% } %>

      Furina do Discord
    </div>
  </header>

  <section class="content">
    <h1>Servidores do Usuário</h1>

    <form action="/atualizar-servidores">
      <button class="update-btn" type="submit">
        🔄 Atualizar servidores
      </button>
    </form>

    <% if (guilds.length === 0) { %>
      <p class="no-guilds">Você não é administrador de nenhum servidor...</p>
    <% } else { %>
      <div class="guilds">
        <% guilds.forEach(guild => { %>
          <div class="guild">
            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png?size=128" alt="Ícone do Servidor" onerror="this.src='/img/sem-servidor.png'">
            <div class="guild-name"><%= guild.name %></div>
            <a href="/dashboard/<%= guild.id %>">Gerenciar</a>
          </div>
        <% }) %>
      </div>
    <% } %>
  </section>

</body>
</html>
