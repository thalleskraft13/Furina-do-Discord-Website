<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Perfil - Furina do Discord</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Raleway:wght@400;600&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Raleway', sans-serif;
      background: radial-gradient(ellipse at top, #0b1c3a 0%, #08172e 70%, #06152a 100%);
      color: white;
      overflow-x: hidden;
    }
    header {
      background-color: rgba(6, 21, 42, 0.9);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .logo {
      display: flex;
      align-items: center;
      font-size: 1.5rem;
      color: #3DD1D9;
      font-weight: bold;
      font-family: 'Playfair Display', serif;
    }
    .logo img {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      margin-right: 10px;
      border: 2px solid #3DD1D9;
    }
    .menu-btn {
      display: none;
      background: none;
      border: none;
      font-size: 1.5rem;
      color: #3DD1D9;
    }
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 220px;
      background-color: #06152a;
      padding-top: 80px;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      padding-left: 1rem;
      z-index: 999;
      transition: all 0.4s ease;
    }
    .sidebar a {
      color: #3DD1D9;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease-in-out;
    }
    .sidebar a:hover {
      color: #ffffff;
      transform: translateX(8px);
    }
    .content {
      margin-left: 240px;
      padding: 3rem 2rem;
    }
    h1 {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      color: #3DD1D9;
      margin-bottom: 2rem;
      text-align: center;
    }
    .tema-grid {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      align-items: center;
    }
    .tema-item {
      background-color: rgba(255, 255, 255, 0.03);
      border: 2px solid #3DD1D9;
      border-radius: 1rem;
      padding: 1.5rem;
      text-align: center;
      position: relative;
      transition: transform 0.3s ease;
      max-width: 600px;
      width: 100%;
      animation: fadeIn 0.8s ease;
    }
    .tema-item:hover {
      transform: scale(1.03);
    }
    .tema-item img {
      width: 100%;
      border-radius: 1rem;
      height: auto;
    }
    .tema-item label {
      display: block;
      margin-top: 0.8rem;
      color: #d2e9ff;
      font-weight: bold;
      font-size: 1.1rem;
    }
    .tema-item .badge {
      position: absolute;
      top: -15px;
      left: 50%;
      transform: translateX(-50%);
      background: #3DD1D9;
      color: #06152a;
      padding: 6px 14px;
      border-radius: 1rem;
      font-size: 0.95rem;
      font-weight: 700;
      box-shadow: 0 0 10px #3DD1D9;
    }
    .tema-item button {
      margin-top: 1rem;
      background: #3DD1D9;
      border: none;
      padding: 0.6rem 1.5rem;
      border-radius: 1rem;
      font-weight: bold;
      color: #06152a;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .tema-item button:hover {
      background-color: #2bb0b7;
      transform: scale(1.05);
    }
    footer {
      background-color: #0a1e33;
      padding: 2rem 1rem;
      text-align: center;
      border-top: 2px solid #3DD1D9;
      margin-top: 3rem;
    }
    footer a {
      margin: 0 1rem;
      text-decoration: none;
      color: #3DD1D9;
      font-size: 1.8rem;
      transition: transform 0.2s;
    }
    footer a:hover {
      color: #ffffff;
      transform: scale(1.2);
    }
    footer p {
      color: #A0CFE8;
      font-size: 0.9rem;
      margin-top: 1rem;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @media (max-width: 768px) {
      .sidebar {
        display: none;
      }
      .content {
        margin-left: 0;
      }
      .menu-btn {
        display: block;
      }
      .sidebar.active {
        display: flex;
        position: fixed;
        left: 0;
        top: 0;
        background-color: #06152a;
        padding-top: 80px;
        flex-direction: column;
        width: 100%;
        height: 100%;
        z-index: 999;
        animation: fadeIn 0.5s ease;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">
      <img src="/img/furina.jpg" alt="Furina Bot">
      Furina do Discord
    </div>
    <button class="menu-btn" onclick="toggleMenu()"><i class="fas fa-bars"></i></button>
  </header>
  <nav class="sidebar" id="sidebar">
    <a href="/dashboard">Início</a>
    <a href="/mochila">Mochila</a>
    <a href="/recompensas">Recompensas Diárias</a>
    <a href="/perfil" class="active">Meu Perfil</a>
    <a href="/eventos">Eventos</a>
    <a href="/loja">Loja</a>
    <a href="/missoes">Missões</a>
    <a href="https://discord.gg/MkneaxC8jY" target="_blank">Servidor Oficial</a>
  </nav>
  <section class="content">
    <h1>Escolha seu Tema</h1>
    <form action="/perfil/tema" method="POST">
      <div class="tema-grid">
        <% temas.sort((a, b) => a.nome === temaAtual ? -1 : b.nome === temaAtual ? 1 : 0).forEach(t => { %>
          <div class="tema-item">
            <input type="radio" name="tema" value="<%= t.nome %>" id="tema-<%= t.nome %>" style="display:none" <%= t.nome === temaAtual ? 'checked' : '' %>>
            <% if (t.nome === temaAtual) { %>
              <div class="badge">Em uso</div>
            <% } %>
            <label for="tema-<%= t.nome %>">
              <img src="/personagem/temas/<%= t.nome %>.png" alt="Tema <%= t.nome %>">
              <span><%= t.nome === "0" ? "Tema Padrão" : t.nome %></span>
            </label>
            <% if (t.nome !== temaAtual) { %>
              <button type="submit" onclick="document.getElementById('tema-<%= t.nome %>').checked = true">Usar</button>
            <% } %>
          </div>
        <% }) %>
      </div>
    </form>
  </section>
  <footer>
    <div>
      <a href="https://youtube.com/@furinadodiscord" target="_blank"><i class="fab fa-youtube"></i></a>
      <a href="https://www.tiktok.com/@furina.do.discord" target="_blank"><i class="fab fa-tiktok"></i></a>
      <a href="https://discord.gg/MkneaxC8jY" target="_blank"><i class="fab fa-discord"></i></a>
    </div>
    <p>Feito por <strong>ThallesKraft</strong> com amor e drama 💧</p>
  </footer>
  <script>
    function toggleMenu() {
      const sidebar = document.getElementById("sidebar");
      sidebar.classList.toggle("active");
    }
  </script>
</body>
</html>